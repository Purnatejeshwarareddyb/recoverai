Place `recoverai_logo.png` in this folder to have it appear in the Streamlit
sidebar. If the file is missing, the app falls back to a plain text title
automatically — nothing breaks.

Recommended: transparent background PNG, roughly 3:1 or 4:1 width:height
ratio so it renders cleanly at sidebar width.
